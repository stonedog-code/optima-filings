{
  "name": "@optima-compliance/ui",
  "version": "0.1.0",
  "description": "Optima presentation layer: the theme, the Lucide icon set, and the app-wide sizing choice.",
  "license": "AGPL-3.0-only",
  "author": "StoneDogCode L.L.C.",
  "type": "module",
  "sideEffects": false,
  "//exports": "Ships TypeScript SOURCE, like @stonedogcode/style and for the same reason: Panda extracts styles by statically parsing source, so a consumer's codegen must be able to read our .tsx. A pre-bundled dist would emit class names Panda never wrote CSS for.",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "prepare:panda": "panda codegen",
    "type-check": "panda codegen && tsc --noEmit"
  },
  "dependencies": {
    "lucide-react": "^0.460.0"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18",
    "@stonedogcode/style": "*"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.12.0",
    "@stonedogcode/style": "^0.20.0",
    "@stonedogcode/theme": "^0.3.0"
  }
}
