{
  "name": "@optima-compliance/web",
  "version": "0.1.0",
  "description": "The self-hosted Optima dashboard. Single-tenant, SQLite, one container.",
  "license": "AGPL-3.0-only",
  "author": "StoneDogCode L.L.C.",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:panda": "panda codegen",
    "predev": "npm run prepare:packages && npm run prepare:panda",
    "dev": "next dev",
    "prebuild": "npm run prepare:packages && npm run prepare:panda",
    "build": "next build",
    "start": "next start",
    "type-check": "panda codegen && tsc --noEmit",
    "prepare:packages": "tsc --build ../../tsconfig.json",
    "//prepare:packages": "engine/rules/db/export declare main:./dist, so they must be COMPILED before Next resolves them. ui and @stonedogcode/style are the opposite \u2014 they ship source and are transpiled by Next."
  },
  "dependencies": {
    "@optima-compliance/db": "0.1.0",
    "@optima-compliance/engine": "0.4.0",
    "@optima-compliance/export": "0.1.0",
    "@optima-compliance/reminders": "0.1.0",
    "@optima-compliance/rules": "2026.8.7",
    "@optima-compliance/ui": "0.1.0",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "server-only": "^0.0.1",
    "@stonedogcode/style": "*"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.12.0",
    "@playwright/test": "^1.62.1",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3"
  },
  "engines": {
    "node": ">=22.5"
  }
}
